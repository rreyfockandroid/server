
server {
    listen 80;
    server_name server.go.pl;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name server.go.pl;

    ssl_certificate /etc/nginx/certs/nginx-go-selfsigned.crt;
    ssl_certificate_key /etc/nginx/certs/private/nginx-go-selfsigned.key;
    ssl_dhparam /etc/nginx/certs/dhparam.pem;

    root /usr/share/nginx/html;
    index server.go.html;

    location / {
        try_files $uri $uri/ =404;
    }
}